import {StyleSheet} from 'react-native';
const addListStyle = StyleSheet.create({
  imageSelector: {
    paddingVertical: 15,
    width: 100,
    // backgroundColor: '#e9967a',
    borderRadius: 10 / 2,
    borderColor: 'black',
    marginBottom: 30,
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
export default addListStyle;
